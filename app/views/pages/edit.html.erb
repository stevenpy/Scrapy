<div class="flex items-center justify-between">
  <h1 class="font-bold text-2xl truncate">
    <%= link_to "←", @page, class: "p-1 rounded hover:bg-gray-100" %>
    Edit page
  </h1>
  <%= button_to "Delete", @page, method: :delete, data: {turbo_confirm: "Are you sure ?"}, class: "border border-gray-200 rounded shadow px-2 py-1 hover:bg-gray-100" %>
</div>
<%= render "form", page: @page %>